services:
  db:
    image: postgres:18-alpine
    container_name: postgres
    environment:
      - POSTGRES_USER=bob
      - POSTGRES_PASSWORD=1234
      - POSTGRES_DB=catdb
    ports:
      - "5432:5432"
  catapp:
    image: catapp-1.0.0
    ports:
      - "8080:8080"